#logical operators (and, or, not) = used to check if two or more condition statmens is true

temp = int(input("what is temperature outside?: "))

if temp >= 0 and temp <= 30:
    print ("temperature is good today!")
    print ("go outside!")
elif temp < 0 or temp > 30:
    print ("temperature is bad today!")
    print ("state inside!")















